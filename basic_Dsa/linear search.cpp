#include<iostream>
using namespace std;
int Search(int n,int k, int array[]);
void Accept(int size,int *arr)
{
    for (int i = 0; i < size; i++)
    {
        cin>>arr[i];
    }
    
}
int main()
{
    int n, array[n], key;
    cout<<"Enter size of array - ";
    cin>>n;
    Accept(n, array);
    cout<<"Enter the key to search - ";
    cin>>

}